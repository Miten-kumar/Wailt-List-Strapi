'use strict';

/**
 * waitlist-survey controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::waitlist-survey.waitlist-survey');
